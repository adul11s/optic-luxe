# Optic Luxe — Development Notes

## Quick Commands

```bash
# Backend (port 3001)
cd server && npm run dev              # http://localhost:3001
cd server && npm test                 # Run unit tests
cd server && npx tsc --noEmit         # Type check

# Frontend (port 3000)
cd client && npm run dev              # http://localhost:3000
cd client && npx tsc --noEmit --skipLibCheck  # Type check
```

## Current Status

- **Server:** 16 modules, 27 DB models, 0 TS errors
- **Client:** 24 pages, 0 TS errors
- **Tests:** 36 passing (Vitest)

## Branch

Main development branch.
